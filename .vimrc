" For syntax highlighting
syntax on

" Sets the tabs to a size of 4
set tabstop=4

" sets the amount of spaces when tab is pressed
set softtabstop=4

" Uses spaces instead of tabs
set expandtab

" Sets absolute numbers
set number

" Stop swapfile
set noswapfile
