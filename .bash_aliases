alias ld='ls -1 | grep ^d' # Lists only directories
alias lf='ls -1 | grep -v /' # Lists only files
alias ll='ls -la' # List everything
alias ls='ls -S' # List by size
alias lt='ls -alt' # List by last modified

alias python='python3'
